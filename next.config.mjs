/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['lh3.googleusercontent.com', 'another-domain.com'], // Replace with your allowed image domains
    },
  };
  
  export default nextConfig;
  